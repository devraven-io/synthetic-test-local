//stubbed methods for available APIs for Synthetic checks
//refer https://docs.devraven.io/docs/synthetic-monitoring/available-apis for more details
const persistValue = (key, value) => {
    //no-op
}

const retrieveValue = (key) => {
    //no-op
}

const deleteValue = (key) => {
    //no-op
};

module.exports = {
    persistValue,
    retrieveValue, 
    deleteValue
};